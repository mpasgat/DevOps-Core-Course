apiVersion: v1
kind: Service
metadata:
  name: devops-python-service
  namespace: devops-lab09
  labels:
    app: devops-python
spec:
  type: NodePort
  selector:
    app: devops-python
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
