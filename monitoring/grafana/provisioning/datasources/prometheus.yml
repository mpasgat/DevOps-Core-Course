apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    jsonData:
      manageAlerts: true
      prometheusType: Prometheus
      prometheusVersion: 3.9.0
      httpMethod: POST
    version: 1
    editable: false
